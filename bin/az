#!/bin/bash

BASE_HOME=${XDG_DATA_HOME:-$HOME/.local/share}
AZ_HOME="${BASE_HOME}/azure"

mkdir -p "${AZ_HOME}"
docker run -it \
	--rm \
	--volume "${AZ_HOME}:/root/.azure" \
	--log-driver none \
	jess/azure-cli "${@}"
