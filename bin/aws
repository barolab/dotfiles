#!/bin/bash

BASE_HOME=${XDG_DATA_HOME:-$HOME/.local/share}
AWS_HOME="${BASE_HOME}/aws"

mkdir -p "${AWS_HOME}"
docker run -it \
	--rm \
	--volume "${AWS_HOME}:/root/.aws" \
	--log-driver none \
	jess/awscli "${@}"
