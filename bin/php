#!/bin/bash

PWD=$(pwd)
GLOBAL="$HOME/.composer"

docker run -t \
	--rm \
	--volume "$PWD:$PWD" \
	--volume "$GLOBAL:$GLOBAL" \
	--workdir "$PWD" \
	--log-driver none \
	--entrypoint php \
	php:alpine "$@"
