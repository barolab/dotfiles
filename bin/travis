#!/bin/bash

BASE_HOME=${XDG_DATA_HOME:-$HOME/.local/share}
TRAVIS_HOME="${BASE_HOME}/travis"

mkdir -p "${TRAVIS_HOME}"
docker run -it \
	--rm \
	--volume "${TRAVIS_HOME}:/root/.travis" \
	--volume "$(pwd):/usr/src/repo:ro" \
	--workdir /usr/src/repo \
	--log-driver none \
	jess/travis "${@}"
